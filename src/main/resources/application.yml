spring:
  application:
    name: customadvice

  datasource:
    url: jdbc:h2:file:./data/testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
